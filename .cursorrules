# Vantage Platform — Cursor Context

Read `CLAUDE.md` at the repo root — it is the single source of truth for product context, architecture, commands, coding standards, and UI conventions. Do not duplicate its content here; update `CLAUDE.md` instead.
